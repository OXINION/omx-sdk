{
  "name": "omx-sdk-examples",
  "version": "1.0.0",
  "private": true,
  "description": "Examples for the OMX SDK",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "serve": "python3 -m http.server 8000",
    "test": "tsx example.ts",
    "test:node": "node --experimental-modules --experimental-top-level-await example.js"
  },
  "dependencies": {
    "omx-sdk": "workspace:*",
    "@supabase/supabase-js": "^2.50.5"
  },
  "devDependencies": {
    "@types/node": "^24.0.12",
    "tsx": "^4.20.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
